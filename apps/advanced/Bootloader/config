# only for MSP430 for now
PLATFORM_ONLY = sadmote telosb epic msp430

USE_FLASH = y
USE_EXT_FLASH = y
USE_LEDS = y
USE_CRC = y
USE_WATCHDOG = y

USE_RADIO = n
USE_SERIAL_NUMBER = n
USE_PRINT = n
USE_HARDWARE_TIMERS = n
USE_KERNEL_MAIN = n
