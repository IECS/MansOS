// 1: read random values from a neighbor, send to serial
// read Random;
// output Radio (Random);
// read RemoteRandom;
// output Serial (RemoteRandom);

// 2: react on a command
//const COMMAND_PRINT 17;
//when RemoteCommand == COMMAND_PRINT:

when RemoteCommand == 17:
//    use print, format "hello world\n", once;
end;

// when Random == 12:
// end;

// define cachedlight light, cache;
// read cachedlight;

//read light;

// define randomBouned random, min 10;
// read randomBouned;
